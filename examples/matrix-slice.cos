import Matrix;

Matrix m1 = [1,2,3];

Matrix m2 = [ [1,2,3], [4,5,6] ];

// 目前 COStream 后端使用 Eigen 库, 对2维数组(矩阵)的支持比较好, 所以建议写成如下表达
Matrix m2_array[3] = {
    [[1,1],[2,2]],
    [[1,1],[2,2]],
    [[1,1],[2,2]]
};

// 这种写法也支持, 但会给切片带来困扰. 如果 numcpp 库的性能很好的话倒是可以这样做
Matrix m3 = [
    [[1,1],[2,2]],
    [[1,1],[2,2]],
    [[1,1],[2,2]]
]; 